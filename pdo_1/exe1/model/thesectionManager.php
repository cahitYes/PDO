<?php

/**
 * Fonction qui sélectionne tous les champs de la table `thesection` ordonnée par `thesectiontitle` ascendant et qui renvoit un tableau vide si pas de résultats
 * @param \PDO $con
 * @return array
 */
function thesectionSelectAll(PDO $con): array
{
    // doit renvoyer un tableau (exemple)
    return [];
}
